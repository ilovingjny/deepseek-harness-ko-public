# Handoff — 한국어 UI 완성 및 dsh-ko 로컬 실행 (2026-08-16)

브랜치: `local/korean-ui` / 저장소: `/Users/yulsol/Documents/mac maintanance/deepseek-harness-ko`
비공개 원격: `origin` = `https://github.com/ilovingjny/deepseek-harness-ko-private.git`

## 이번 세션에서 한 일

1. `packages/client/locale/tests/korean-coverage.spec.ts` lint 오류 6건 수정
   - 67행 max-len (조건식 줄바꿈)
   - 121·137·203·221행 arrow-parens (`(property) => {`, `(node) => {` 블록 바디 괄호)
   - 111행 no-confusing-void-expression (`child => walk(child, visit)` → 블록 바디)
2. `apps/web/tests/background-job-list.e2e.ts` 경합 버그 1줄 수정
   - 원인: Playwright `getByRole` name 옵션은 기본 부분 문자열 매칭이라 `name: '1 background job'`이 실행 중 라벨 `'1 background job running'`에 즉시 매칭 → `waitFor`가 정착을 기다리지 못하고, 킬 후 ~55ms 전이(프로브로 측정) 전에 aria 스냅샷이 `running`을 캡처 → 골든 불일치.
   - 수정: `{ name: '1 background job', exact: true }` + 이유 주석. 상위 프로젝트에서 물려받은 테스트이며 한국어 변경과 무관한 기존 경합.
3. 로컬 실행 환경 구축
   - `~/.local/bin/dsh-ko` (755): `local-tools/dsh-ko` 사본 (DSH_HOME=~/.dsh-ko, 인자 없으면 `web`)
   - `~/.dsh-ko/` 전용 홈: `settings.yaml`은 `local-tools/settings.ko.yaml` 사본 (OpenCode Go 설정, locale.preference=ko), `.credentials.yaml`은 `~/.dsh/.credentials.yaml`을 권한 600 보존해 복사 (내용은 출력·노출 안 함)
   - `/opt/homebrew/bin/dsh`와 `~/.dsh`는 무변경 보존 확인

## 검증 결과 (모두 통과)

| 검증 | 명령 | 결과 |
|---|---|---|
| 한국어 집중 테스트 | `pnpm vitest run packages/client/locale/tests packages/client/ui-* …` (12개 패키지) | 855 passed |
| GUI 테스트 | `pnpm run test:gui` | 3761 passed, 4 skipped |
| 타입 검사 | `pnpm run typecheck` | 통과 (rc=0) |
| lint | `pnpm run lint` | 0 warnings, 0 errors (2461 files) |
| 문서 검사 | `pnpm run doc-sync` | 28 passed, 0 failed |
| 웹 replay e2e | `DSH_SNAPSHOT=replay pnpm run test:web` | 254 passed, 15 skipped (최종 실행 EXIT=0) |

- Playwright 브라우저는 `pnpm --filter @deepseek-ai/dsh-web-frontend exec playwright install chromium`으로 설치 (headless shell).
- `test:web` 이력: 1차 실행에서 background-job-list 골든 불일치 → exact:true 수정 후 통과. 2~3차에서 minimal-preset.snapshot.ts의 afterAll `agentHandle.dispose()` 120초 타임아웃 1회 발생(테스트 본체는 통과) → 단독 실행 14.5초로 통과, 이후 전체 실행에서 재발 없음. **부하 플레이크로 판단(분석 서브에이전트 결론 확인 필요)**.

## ko-KR 브라우저 실제 확인 (dsh-ko 웹 서버 대상, Playwright)

- 서버: `~/.local/bin/dsh-ko web --port 3081` 기동 → `http://127.0.0.1:3081` HTTP 200. 참고: 기본 포트 3080은 이 GUI 세션(DeepSeek Harness Web GUI)이 점유 중이라 EADDRINUSE — 환경 충돌이지 제품 결함 아님. 프로필 부팅 자체는 정상.
- 검증 결과 (locale ko-KR, 저장소 기본값):
  - 설정 버튼 `설정` 표시: true
  - 현재 언어 `한국어` 표시: true
  - 언어 메뉴 `한국어` 1 / `English` 1 / `中文` 1
  - 주요 화면 중국어 잔여물: 없음 (body 내 CJK 실행 0건)
  - 저장된 로케일 핀 없음(브라우저 자동 감지), 페이지 오류 0건
- `dsh --version` = `0.1.0-rc.6` (공식판 그대로), `dsh-ko --version` = `0.1.0-rc.5`, `dsh-ko web --help` 동작, `dsh-ko` 인자 없음 → 웹 프로필 시작 확인.

## 커밋 상태

- 이 문서 작성 시점: 커밋·push **미완료**. 커밋 대상:
  - 이전 세션 변경(로케일 사전/시트 이동, e2e, 에이전트 노트, locale README) + 이번 세션 수정 2건
  - 신규: `packages/client/locale/tests/korean-coverage.spec.ts`, `local-notes/`, `local-tools/`
- 커밋 메시지 제안: `feat(locale): finish Korean UI verification with local dsh-ko launcher`

## 남은 일 / 알려진 문제

1. **커밋 + push**: `git add` → `git commit` → `git push origin local/korean-ui` (검증 전부 끝난 뒤에만)
2. **minimal-preset dispose hang 플레이크**: 전체 스위트 부하 중 `afterAll` dispose가 120초 초과 1회. 단독/후속 실행 통과. 분석 서브에이전트(35b4be26) 결론을 handoff에 반영할 것. 필요하면 CI/상위 저장소에서 같은 플레이크 이력 확인.
3. **ui-trajectory 정적 영어 문구** (이번 완료 조건 아님, 별도 후속): `Input`, `Model`, `Tools`, `No timing data`, `Event details`, `Status`, `Provider`, `System Prompt`, `Tool Call` 등 — `local-notes/korean-ui-audit.ko.md` 참조. 타입 안전한 중간 상태를 만들지 않기 위해 미수정.
4. **pro 모델 분석 시도 실패**: workflow 툴로 `deepseek-v4-pro`(provider deepseek-official) 단일 에이전트를 호출했으나 자식 실패(null)로 종료됨. 모델/프로바이더 식별자 또는 에이전트 설정 문제일 수 있음. 후속 세션에서 검증 필요.
5. **기타 플레이크 후보**: `settings-chrome.e2e.ts` 한국어 테스트 포함 9/9 통과 확인됨. `markdown-cjk-strong`, `navigation-panes` 등 부하 시 재확인 권장.

## 환경 메모

- `~/.dsh-ko/settings.yaml`은 로케일 preference `ko` 포함 — dsh-ko 웹은 브라우저 로케일과 무관하게 한국어 UI로 시작.
- `dsh-ko`는 `pnpm --dir <repo> dsh` 소스 런치(tsx). 웹 dist는 `pnpm run build:web`(또는 test:web의 build 단계)로 생성돼 있어야 서빙 가능.
- 공식 `~/.dsh/.credentials.yaml`은 내용 미노출, 600 권한으로 `~/.dsh-ko/.credentials.yaml`에 복사됨.
